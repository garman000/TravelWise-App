// import clientPromise from "../../../../lib/mongodb";

// export default async (req, res) => {
//   const dateUntil = JSON.parse(req.body);
//   const client = await clientPromise;
//   const db = client.db();
//   //   let tripDate = await db.collection("date_Until").insertOne(dateUntil);
//     res.json({ tripDate });
// };
